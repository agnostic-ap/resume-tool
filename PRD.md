# Resume Tool PRD

版本：v0.1  
日期：2026-05-23  
作者：Codex 产品审阅

## 1. 产品定位

Resume Tool 是一个面向求职者和平台方的「简历与求职工作台」。它不应只停留在简历编辑和 PDF 导出，而应帮助用户完成从职业素材沉淀、简历生成、JD 定制、投递跟进到结果复盘的完整闭环。

当前项目已经具备较好的 MVP 基础：

- C 端简历编辑器：多章节编辑、实时预览、模板切换、风格调整、PDF 导出。
- 工作台：简历库、模板、成长清单、投递管线、历史记录、设置。
- 后端 API：简历、投递、活动记录、平台草稿生成接口。
- 管理端：超管控制台视觉框架。

但从产品完整度看，现状更像「多个功能模块的集合」，关键用户路径还没有闭环，部分入口承诺和实际落地能力不一致，保存/同步/导出等信任状态也不够清晰。

## 2. 核心问题

### 2.1 首次使用路径不清晰

应用默认展示示例数据，视觉上完整，但掩盖了真实空白状态。用户新建空白简历后，缺少明确的字段填写顺序和完成指引。

影响：

- 用户不知道先填个人信息、经历、技能还是直接选模板。
- 空白简历体验弱，容易依赖 demo 数据撑起产品完整感。
- 完整度建议偏泛化，不能直接驱动下一步操作。

### 2.2 AI 定制入口和真实能力不一致

工作台存在「AI 定制」心智，但用户进入编辑器后看到的是本地文本建议。真正的 JD 定制流程已经写在 `WorkspacePanel.vue`，包含 JD 输入、草稿生成、章节对比、应用草稿、创建投递记录，但 UI 被隐藏。

影响：

- 用户点击「根据 JD 定制」却没有得到结构化 JD 简历优化。
- 已有后端 `generateAssistantResumeDraft` 能力没有形成主路径。
- AI 价值无法转化为简历版本和投递记录。

### 2.3 求职投递管线不够像生命周期管理

当前投递模块已有公司、岗位、阶段、匹配度、跟进日期、备注和进度日志，但主要仍是表格心智。

影响：

- 用户无法围绕单个岗位查看完整详情和时间线。
- 进度事件不能独立编辑日期、阶段、标题和备注。
- 跟进日期没有形成「今日待办 / 逾期提醒」。
- 从 JD 定制草稿到投递记录的链路没有被产品化。

### 2.4 职业成长记录没有沉淀为长期记忆

当前成长模块是双周清单，完成后会重置。对求职产品而言，职业成长记录应是后续生成简历和匹配 JD 的高价值素材库。

影响：

- 用户记录的项目、指标、技能成长无法长期复用。
- AI 无法引用历史成长素材生成更强的简历内容。
- 产品缺少持续使用的理由。

### 2.5 保存、同步、导出状态不可信

顶部 Saved 状态主要监听简历内容变更，不能完整表达文档元数据、投递、设置、导入、后端失败、重试队列等状态。导出前虽然有分页线和页数提示，但缺少正式的预检与修复建议。

影响：

- 用户可能误以为所有内容已经保存。
- 后端失败时缺少可见反馈和重试机制。
- 导出的 PDF 可能出现分页断裂、联系方式溢出、章节跨页等问题。

### 2.6 管理端和平台 API 仍停留在演示态

管理端目前是静态数据面板，菜单和按钮没有真实动作。平台 API 已有草稿接口和请求记录，但缺少 OpenAPI 文档、客户端密钥、配额、请求日志详情和契约测试。

影响：

- 平台所有者无法真实运营用户、简历、投递和 API 调用。
- 外部系统接入成本高，缺少可信的集成文档。
- API 权限和审计能力不足，不适合生产化。

## 3. 目标用户

### 3.1 求职者

需要快速创建、优化、导出不同岗位版本的简历，并持续跟踪岗位投递进展。

核心诉求：

- 快速从空白、导入或示例开始。
- 能根据 JD 定制简历。
- 能管理不同岗位的简历版本。
- 能导出稳定、专业、不出错的 PDF。
- 能跟踪投递进度和后续行动。

### 3.2 有长期职业规划的用户

希望持续记录工作成果、指标、项目和技能成长，在换工作时快速转化为简历素材。

核心诉求：

- 低成本记录职业成果。
- 能按项目、技能、指标组织素材。
- 简历生成时能复用历史素材。

### 3.3 平台运营 / 超管

需要管理用户、文档、投递、平台调用、API Key、审计日志和部署健康。

核心诉求：

- 看真实数据，而不是静态样例。
- 能控制权限、密钥和危险操作。
- 能追踪平台 API 调用质量和失败原因。

### 3.4 外部系统开发者

希望通过 API 输入用户履历和 JD，生成可保存、可追踪的简历草稿。

核心诉求：

- 有稳定公开的 API 契约。
- 有认证、幂等、错误码和示例。
- 能查看请求结果、生成文档和失败原因。

## 4. 产品目标

### 4.1 近期目标

在现有代码基础上，优先打通 C 端真实求职闭环：

1. 首次创建：空白 / 导入 / 示例三路径。
2. 编辑简历：字段引导、完整度、模板和风格实时预览。
3. JD 定制：粘贴 JD，生成结构化草稿，按章节应用。
4. 投递管理：从草稿创建投递记录，跟进进度和提醒。
5. 导出检查：导出前发现并修复明显问题。

### 4.2 中期目标

把工具升级成可持续使用的职业资产库：

1. 成长记录从清单升级为 append-only 职业记忆。
2. 简历库支持搜索、批量管理、版本来源和使用关系。
3. AI 定制可引用简历、JD、成长记录、投递反馈。
4. 保存和同步状态可恢复、可解释、可重试。

### 4.3 长期目标

形成可运营、可集成的平台产品：

1. 管理端接入真实数据和权限体系。
2. 平台 API 提供 OpenAPI、密钥、配额、审计和用量统计。
3. 后端从单 JSON 文件存储迁移到具备用户/工作区隔离的持久数据库。

## 5. 成功指标

### 5.1 激活指标

- 新用户首次进入后，完成一份可导出简历的比例。
- 空白创建用户完成个人信息、经历、技能三类核心字段的比例。
- 首次会话内导出 PDF 的比例。

### 5.2 AI 定制指标

- JD 定制入口点击率。
- JD 草稿生成成功率。
- 用户采纳 AI 建议的章节比例。
- 从 JD 草稿创建投递记录的比例。

### 5.3 留存指标

- 用户创建 2 份及以上简历版本的比例。
- 用户记录成长条目的频次。
- 投递记录有后续进度更新的比例。

### 5.4 质量指标

- PDF 导出成功率。
- 导出预检问题修复率。
- 后端同步失败率和重试成功率。
- 平台 API P95 响应时间、错误率、幂等命中率。

## 6. 功能范围与优先级

### P0：打通求职核心闭环

#### 6.1 首次使用与空白简历引导

需求：

- 首次进入展示三种开始方式：空白创建、导入备份、使用示例。
- 空白简历进入编辑器后展示引导清单。
- 引导清单至少包含：个人信息、求职标题、个人简介、最近经历、技能、导出检查。
- 点击清单项可以跳转并高亮对应编辑区。
- 空章节需要有明确主操作，而不是仅展示空白卡片。

验收标准：

- 新用户不依赖示例数据也能理解下一步。
- 空白简历至少能在 5 分钟内完成核心字段并导出。
- 完整度建议必须能落到具体字段或章节。

#### 6.2 编辑器内 JD 定制流程

需求：

- 在编辑器右侧或抽屉中提供 JD 定制入口。
- 支持输入公司、岗位、地点、JD 正文、要求和关键词。
- 调用 `backendApi.generateAssistantResumeDraft` 生成草稿。
- 展示结构化结果：匹配分、命中关键词、使用策略、生成时间。
- 按章节展示 before / after，包括个人简介、经历、技能、项目。
- 用户可以逐项采纳、跳过或全部应用。
- 应用后可创建关联投递记录，写入 `tailoring` 元数据。

验收标准：

- 工作台「AI 定制」进入后必须落到真实 JD 流程。
- AI 开关关闭时，所有 AI 卡片和 JD 定制入口遵循同一可见性策略。
- 生成草稿失败时展示错误原因和重试按钮。
- 已应用的章节必须可追踪到来源 request id。

#### 6.3 投递详情与跟进生命周期

需求：

- 投递列表保留表格，同时新增详情抽屉或详情页。
- 详情中展示公司、岗位、阶段、匹配度、JD 快照、使用简历、AI tailoring 元数据。
- 支持添加、编辑、删除进度事件。
- 进度事件字段包括：日期、阶段、标题、备注。
- 支持设置下一步行动和 follow-up 日期。
- 提供今日待跟进、逾期、高匹配岗位视图。

验收标准：

- 用户可以从「待投递」一路更新到「Offer / 关闭」。
- 逾期 follow-up 必须有明显状态。
- 从 JD 草稿创建的投递记录能回溯到简历草稿和 JD 快照。

#### 6.4 导出前预检

需求：

- 导出前运行预检，不只依赖视觉分页线。
- 检查项包括：联系方式过长、核心章节为空、章节跨页、内容溢出、PDF 页数异常。
- 对每类问题给出建议操作：紧凑密度、减小字号、隐藏低价值章节、编辑内容、继续导出。
- 严重问题需要二次确认后才能继续导出。

验收标准：

- 用户导出前能看到问题列表和影响范围。
- 对可自动修复的问题，产品提供一键应用。
- 导出成功后记录 activity，失败时显示可重试错误。

#### 6.5 保存与同步状态

需求：

- 区分本地保存、云端同步中、云端失败、离线模式。
- 简历内容、简历元数据、投递、成长记录、设置、导入都应进入统一同步状态。
- 后端写入失败时进入失败队列，支持重试。
- 顶部状态不能在仍有失败或待同步操作时显示单纯 Saved。

验收标准：

- 断开后端时，用户明确知道当前是本地模式。
- 后端恢复后可重试同步。
- 每个失败操作能看到对象类型、操作类型和错误原因。

### P1：提升资产管理和持续使用价值

#### 6.6 职业成长记忆

需求：

- 将成长清单升级为 append-only 成长条目。
- 字段包括：日期、类型、公司/项目、内容、量化指标、技能、证据链接、隐私标记、来源简历。
- 支持筛选、搜索、编辑和归档成长条目。
- JD 定制时允许选择要引用的成长条目。
- 展示条目使用状态：未使用、已用于某份简历、已用于某次投递。

验收标准：

- 完成一次成长记录不会清空历史。
- AI 定制请求能携带被选择的成长素材。
- 用户能查看某条成长记录被哪些简历使用过。

#### 6.7 简历库增强

需求：

- 支持搜索、排序、批量归档、批量删除。
- 展示简历来源：空白创建、导入、复制、JD 草稿生成。
- 展示关联投递、最近导出、最近编辑时间。
- 编辑器选择器默认排除已归档简历，已归档简历进入只读或需先恢复。

验收标准：

- 用户能快速找到某家公司/岗位对应的简历版本。
- 批量删除和导入覆盖必须有确认和撤销。
- 简历 lineage 不因复制或 AI 生成而丢失。

#### 6.8 命令面板数据化

需求：

- 命令面板不仅搜索静态命令，也能搜索最近简历、投递公司、成长条目和模板。
- 支持直接执行：打开简历、进入投递详情、开始 JD 定制、切换模板、导出。

验收标准：

- 用户输入公司名能打开对应投递记录。
- 用户输入模板名能直接切换并预览。
- 命令结果按上下文和最近使用排序。

#### 6.9 模板质量与视觉回归

需求：

- 10 个模板需要有明确布局意图和适用场景。
- 缩略图、实时预览和 PDF 导出结果保持一致。
- 为模板增加视觉回归测试。

验收标准：

- 每个模板视觉上有可感知差异。
- 至少覆盖中文、英文、长经历、长技能列表四类测试数据。
- 导出 PDF 不出现明显裁切和重叠。

### P2：平台化与工程治理

#### 6.10 管理端真实化

需求：

- 接入超管登录和角色权限。
- 左侧菜单切换真实页面。
- 接入用户/租户、简历、投递、平台请求、API Key、部署健康、配置同步、审计日志。
- 危险操作需要二次确认并写入审计日志。

验收标准：

- 管理端不再使用静态数组作为主要数据源。
- 非超管用户不能访问敏感页面。
- 每个写操作可追踪操作者、时间、对象和结果。

#### 6.11 平台 API 产品化

需求：

- 为 `/api/v1/resume-drafts` 提供 OpenAPI 文档。
- 提供 curl 示例、请求/响应示例、错误码和幂等说明。
- 支持 per-client API Key、scope、quota、rate limit。
- 管理端可查看请求详情：client、request id、路由、耗时、状态、失败原因、生成 document id。

验收标准：

- 外部开发者可以仅凭文档完成一次调用。
- 无有效 key 的请求被拒绝。
- 超管可按 client 过滤请求日志和用量。

#### 6.12 数据模型与持久化

需求：

- 从单 JSON 文件持久化迁移到数据库。
- 引入用户、工作区、简历、投递、成长记录、活动、平台请求等实体表。
- 所有实体具备 user/workspace ownership。
- 增加 revision/version 字段处理多端冲突。
- 导入备份增加 schemaVersion、预览、校验报告和迁移日志。

验收标准：

- 不同用户之间数据隔离。
- 旧客户端或旧备份导入有明确迁移路径。
- 并发编辑不会静默覆盖。

## 7. 非功能需求

### 7.1 可用性

- 核心编辑、预览、导出路径在桌面端必须稳定。
- 窄屏需要明确支持策略：适配平板/手机，或展示受支持设备提示。
- 所有 destructive action 必须有确认，关键操作尽量支持短时撤销。

### 7.2 性能

- 编辑器输入响应应保持流畅。
- PDF 导出依赖应按需加载，避免拖慢首屏。
- 平台 API 需要记录 latency，并定义 P95 目标。

### 7.3 国际化

- 简历内容可以由用户自行维护语言。
- 产品 UI、状态、按钮、错误、空状态、进度标题必须进入 locale 层。
- 管理端需明确是中文专用，或补齐国际化能力。

### 7.4 安全与合规

- API Key 不应只有全局环境变量模式。
- 管理端必须有认证、授权和审计。
- 用户简历、投递、成长记录属于高敏个人数据，需要避免误共享和越权访问。

## 8. 推荐里程碑

### M1：C 端闭环修复

范围：

- 首次使用三路径。
- 编辑器 JD 定制。
- 投递记录从 JD 草稿创建。
- 导出预检基础版。
- 保存/同步状态基础版。

交付判断：

- 用户能从空白简历开始，根据 JD 定制内容，创建投递记录，并导出通过预检的 PDF。

### M2：职业资产与投递管理

范围：

- 成长记录 append-only。
- 投递详情抽屉和可编辑时间线。
- 简历库搜索、批量和 lineage。
- 命令面板数据化。

交付判断：

- 用户能持续记录成长素材，并在不同岗位简历中复用。

### M3：平台与管理端生产化

范围：

- 管理端真实数据和权限。
- 平台 API OpenAPI 文档。
- API Key、quota、rate limit、审计日志。
- 数据库存储和用户/工作区隔离。

交付判断：

- 产品具备面向外部系统和真实运营的基础能力。

## 9. 风险与依赖

### 9.1 产品风险

- 功能过多导致主路径被淹没，应优先保证「简历编辑 -> JD 定制 -> 投递 -> 导出」闭环。
- AI 能力如果仍是规则生成，需要避免过度承诺「智能优化」。
- 成长记录如果录入成本过高，可能无法形成留存。

### 9.2 技术风险

- `WorkspacePanel.vue` 过大，继续加功能会增加回归风险。
- 前后端类型分离，API schema 容易漂移。
- JSON 文件存储不适合多用户、多端和平台化。
- PDF 导出基于截图切片，复杂分页质量难以保证。

### 9.3 体验风险

- 示例数据过强会掩盖空状态。
- 保存状态不准确会破坏用户信任。
- 管理端如果长时间保持静态演示，会造成产品边界误判。

## 10. 不完善点汇总

| 优先级 | 模块 | 不完善点 | 建议动作 |
| --- | --- | --- | --- |
| P0 | 首次使用 | 空白简历缺少引导 | 增加 blank/import/sample 和字段清单 |
| P0 | AI 定制 | 入口承诺与实际能力不一致 | 恢复真实 JD 草稿流程到编辑器 |
| P0 | 投递管线 | 表格强，生命周期弱 | 增加详情抽屉、时间线、跟进提醒 |
| P0 | 导出 | 缺少预检和修复建议 | 增加导出前检查和一键修复 |
| P0 | 保存同步 | Saved 状态不覆盖所有写入 | 建立统一同步队列和失败重试 |
| P1 | 成长记录 | 清单会重置，不能复用 | 改为 append-only 职业记忆 |
| P1 | 简历库 | 搜索、批量、来源关系不足 | 增加搜索、lineage、批量操作 |
| P1 | 命令面板 | 只能搜静态命令 | 接入简历、投递、模板、成长数据 |
| P1 | 模板 | 10 个模板差异和 QA 不足 | 定义模板意图并加视觉回归 |
| P2 | 管理端 | 静态数据，无真实控制权 | 接入认证、真实 API、审计 |
| P2 | 平台 API | 缺少文档、密钥、配额 | 发布 OpenAPI 和 client key 管理 |
| P2 | 数据模型 | JSON 文件存储不适合生产 | 迁移数据库并加用户/工作区隔离 |

## 11. 当前版本建议结论

当前项目的方向是成立的，且已经有不少高价值能力的雏形。最大问题不是「没有功能」，而是「功能没有组成可信的用户闭环」：

- JD 定制能力存在但不可见。
- 成长记录存在但不能沉淀。
- 投递管线存在但不像工作流。
- 管理端存在但没有控制权。
- 保存和导出存在但信任状态不完整。

因此下一阶段不建议继续横向堆新模块，应集中修复 P0 闭环，把产品从「简历编辑器」推进到「求职工作台」。当用户能顺畅完成一条真实路径：创建简历、按 JD 优化、生成岗位版本、记录投递、导出可信 PDF，产品价值会比现在清晰很多。
