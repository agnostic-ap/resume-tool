# Resume Tool 执行路线图

产出日期：2026-05-24  
角色视角：资深产品经理审阅  
参考材料：`PRD.md`、`TODO.md`、`docs/user-flow-audit.md`、当前前端/后端/管理端代码

## 1. 审阅结论

当前项目方向成立，且已经具备简历编辑、模板预览、PDF 导出、投递记录、平台草稿 API、管理端视觉框架等 MVP 基础。主要问题不是“缺功能”，而是“功能没有形成可信闭环”。

最需要优先解决的产品断点：

| 优先级 | 模块 | 当前不完善点 | 对用户的影响 | 执行判断 |
| --- | --- | --- | --- | --- |
| P0 | 首次使用 | 欢迎页已有“示例/空白”，但缺少导入路径和空白后的字段引导 | 用户进入空白简历后不知道先填什么 | 先补空白引导和导入预览 |
| P0 | JD 定制 | `App.vue` 已有编辑器 JD 草稿逻辑但无可见入口；`WorkspacePanel.vue` 的完整 JD UI 仍被 `v-if="false"` 隐藏 | 用户点击 AI 定制后无法稳定完成真实 JD 优化 | 收敛为编辑器右侧主路径 |
| P0 | AI 预期 | 可见 AI 助手仍偏本地建议，和“基于 JD 定制”的承诺不一致 | 信任损耗，采纳率低 | 区分“文本建议”和“JD 定制草稿” |
| P0 | 投递管线 | 表格和进度备注已有，但没有详情页、可编辑时间线、逾期提醒 | 不像生命周期管理工具 | 增加投递详情抽屉和跟进视图 |
| P0 | 保存同步 | 顶部 Saved 主要监听简历内容变化，后端失败只变 offline，没有失败队列 | 用户误以为所有内容已保存 | 建统一同步状态和重试中心 |
| P0 | 导出 | `html2canvas + jsPDF` 直接截图切页，缺少导出前预检 | PDF 可能分页错乱、溢出 | 加预检、严重问题二次确认 |
| P1 | 成长记录 | 仍是会重置的 checklist，不是长期职业素材库 | 无法沉淀后续 AI 可复用资产 | 改为 append-only growth entries |
| P1 | 简历库 | 缺少搜索、批量、版本来源、关联投递关系 | 多版本管理困难 | 补 lineage 和批量管理 |
| P1 | 命令面板 | 只搜静态命令，不搜业务对象 | 高级用户效率有限 | 接入简历、岗位、模板、成长条目 |
| P1 | 模板质量 | 10 个模板未形成足够可感知差异和视觉回归保障 | 模板承诺大于体验 | 定义模板意图和测试样例 |
| P2 | 管理端 | `code/admin/src/App.tsx` 使用静态数组，菜单和按钮无真实动作 | 平台方没有真实控制权 | 接入认证、真实 API、审计 |
| P2 | 平台 API | 有草稿接口和请求记录，但缺 OpenAPI、per-client key、配额 | 外部接入不可运营 | 做 API 产品化 |
| P2 | 数据模型 | SQL 已有草案，运行态仍以 JSON 文件存储为主 | 不适合多用户、多端、生产化 | 分阶段迁移数据库 |

## 2. 产品范围取舍

未来 6-8 周不要继续横向堆模块。主目标只有一个：让真实求职者完成一条顺滑路径。

目标路径：

1. 选择空白、导入或示例开始。
2. 完成核心字段，看到明确完整度和下一步。
3. 粘贴 JD，生成结构化草稿。
4. 按章节采纳差异，形成岗位版本。
5. 一键创建关联投递记录。
6. 设置跟进和记录进展。
7. 导出前完成预检并得到稳定 PDF。

暂缓项：

- 不先做复杂商业化、支付、会员体系。
- 不先做完整平台开放生态。
- 不先做大型管理端权限矩阵。
- 不先把所有模板重做成独立大版本。

## 3. 里程碑规划

建议采用 2 周一个迭代，先连续完成 3 个里程碑。

| 里程碑 | 周期 | 目标 | 交付判断 |
| --- | --- | --- | --- |
| M0：收敛与埋点 | 第 1 周 | 对齐当前真实状态，建立验收口径 | 有事件埋点清单、P0 任务拆分、测试基线 |
| M1：C 端求职闭环 | 第 2-4 周 | 打通创建、编辑、JD 定制、投递、导出 | 用户可从空白简历完成一次 JD 定制并导出 |
| M2：职业资产与投递生命周期 | 第 5-7 周 | 强化成长记录、投递详情、简历库管理 | 用户可沉淀素材并复用到多岗位版本 |
| M3：平台化最小闭环 | 第 8-10 周 | 管理端接真实数据，API 有文档和密钥 | 平台方可查看真实调用并管理 API client |

## 4. M0：收敛与埋点

### 4.1 产品对齐

负责人：产品  
协作：前端、后端、设计、测试

交付件：

- 一份 P0 流程图：首次进入 -> 编辑器 -> JD 定制 -> 应用草稿 -> 创建投递 -> 导出。
- 一份页面状态清单：空状态、加载中、失败、离线、后端未连接、同步失败、导出警告。
- 一份事件埋点表。

关键埋点：

| 事件 | 触发时机 | 核心属性 |
| --- | --- | --- |
| `onboarding_choice_selected` | 用户选择开始方式 | `choice` |
| `resume_core_field_completed` | 完成核心字段 | `field`, `resume_id` |
| `jd_draft_requested` | 点击生成 JD 草稿 | `resume_id`, `has_company`, `jd_length` |
| `jd_draft_generated` | 草稿生成成功 | `request_id`, `score`, `matched_keyword_count` |
| `jd_section_applied` | 采纳章节 | `section`, `request_id` |
| `application_created_from_jd` | 从草稿创建投递 | `application_id`, `request_id` |
| `export_precheck_completed` | 导出预检完成 | `issue_count`, `blocking_count` |
| `sync_operation_failed` | 任一后端写入失败 | `entity_type`, `operation`, `error_code` |

验收标准：

- P0 每个任务都有明确 owner、开发入口、验收标准。
- 后续所有 P0 上线都能被事件追踪验证。

## 5. M1：C 端求职闭环

### 5.1 首次使用与空白引导

负责人：前端  
协作：产品、设计、测试

范围：

- 欢迎页补齐三路径：空白创建、导入备份、浏览示例。
- 空白简历进入编辑器后展示引导清单。
- 引导项至少包括：个人信息、求职标题、个人简介、最近经历、技能、导出检查。
- 点击引导项跳转到对应编辑区并高亮。
- 空章节显示主操作，不依赖 demo 数据撑场面。

验收标准：

- 新用户选择空白后，5 分钟内能完成核心字段并看到导出入口。
- 完整度建议必须指向具体字段或章节。
- 导入路径必须有预览和确认，不允许直接覆盖。

### 5.2 编辑器 JD 定制主路径

负责人：前端  
协作：后端、产品、测试

范围：

- 将 JD 定制入口放到编辑器右侧或抽屉，作为主路径。
- 复用 `backendApi.generateAssistantResumeDraft`。
- 展示公司、岗位、JD 正文、关键词、匹配分、策略、生成时间。
- 按章节展示 before / after：个人简介、经历、技能、项目。
- 支持单项采纳、全选、清空、全部应用。
- 支持从草稿创建投递记录，并写入 `tailoring` 元数据。
- 清理或合并 `WorkspacePanel.vue` 中被隐藏的 JD UI，避免双实现漂移。

验收标准：

- 用户从工作台点击“AI 定制”后能落到真实 JD 流程。
- 生成失败展示后端错误和重试按钮。
- `showAI=false` 时，AI 分数、AI 文本建议、JD 定制入口遵循同一可见性策略。
- 应用章节后，activity 记录包含 `request_id` 和章节信息。

### 5.3 AI 体验降噪

负责人：产品、前端  
协作：后端

范围：

- 将“AI 编辑助手”明确定位为轻量文本建议。
- 将“JD 定制草稿”明确定位为结构化草稿。
- 文案避免承诺真实大模型能力，除非后端已接入。
- 对规则生成结果加说明：基于关键词、经历和岗位描述生成。

验收标准：

- 用户不会把本地文本建议误解成 JD 定制。
- 所有 AI 入口有统一的关闭策略、失败态和可重试态。

### 5.4 投递记录联动

负责人：前端  
协作：产品、后端

范围：

- 从 JD 草稿创建投递记录时保存 JD 快照、匹配分、命中关键词、request id、使用简历。
- 投递列表中显示 JD 来源 chip。
- 新建投递后跳转到投递页并定位到该记录。

验收标准：

- 投递记录可回溯到生成它的简历和 JD 草稿。
- 后续编辑简历不影响已归档 JD 快照。

### 5.5 导出前预检

负责人：前端  
协作：设计、测试

范围：

- 导出按钮前增加预检步骤。
- 检查：核心章节为空、联系方式过长、章节跨页、内容溢出、PDF 页数异常。
- 给出修复建议：紧凑密度、减小字号、隐藏低价值章节、返回编辑、继续导出。
- 严重问题二次确认。

验收标准：

- 至少覆盖中文长内容、英文长内容、联系方式超长、经历过多四类样本。
- 预检失败不会直接导出。
- 导出成功和失败都写入 activity。

### 5.6 保存与同步状态

负责人：前端、后端  
协作：测试

范围：

- 建立统一 sync operation 状态：pending、synced、failed、local-only。
- 纳入对象：简历内容、简历元数据、投递、成长记录、设置、导入。
- 顶部状态区区分：本地已保存、云端同步中、云端失败、离线本地模式。
- 失败操作进入队列，支持手动重试。

验收标准：

- 后端断开时不显示单纯 Saved。
- 后端恢复后可以重试失败操作。
- 每个失败操作显示对象、操作、时间、错误原因。

## 6. M2：职业资产与投递生命周期

### 6.1 投递详情抽屉

负责人：前端  
协作：产品、设计、后端

范围：

- 投递表格保留，新增详情抽屉。
- 展示公司、岗位、阶段、匹配度、JD 快照、使用简历、tailoring 元数据、联系人、链接、备注。
- 支持编辑进度事件：日期、阶段、标题、备注。
- 支持删除进度事件。
- 增加今日待跟进、逾期、高匹配岗位视图。

验收标准：

- 用户可从待投递推进到 Offer / Rejected。
- 逾期 follow-up 有明显视觉状态。
- 时间线事件不是只能追加备注，而是可编辑结构化事件。

### 6.2 成长记录升级为职业记忆

负责人：前端、后端  
协作：产品、测试

范围：

- 新增 append-only growth entries 数据模型。
- 字段：日期、类型、公司/项目、标题、内容、量化指标、技能、证据链接、隐私标记、来源简历。
- 支持搜索、筛选、编辑、归档。
- JD 定制请求允许选择引用的成长条目。
- 展示使用状态：未使用、已用于某简历、已用于某投递。

验收标准：

- 点击“已更新”不再清空历史。
- 用户可选择成长条目参与 JD 定制。
- 每条成长记录可查看被哪些简历或投递使用。

### 6.3 简历库增强

负责人：前端  
协作：后端、测试

范围：

- 搜索、排序、批量归档、批量删除。
- 展示来源：空白、导入、复制、JD 草稿生成。
- 展示关联投递、最近导出、最近编辑时间。
- 编辑器选择器默认排除已归档简历，归档简历进入只读或需先恢复。

验收标准：

- 用户能通过公司、岗位、标签找到对应简历版本。
- 复制和 JD 生成不会丢失 lineage。
- 批量删除有确认和短时撤销。

### 6.4 命令面板数据化

负责人：前端  
协作：产品

范围：

- 命令面板接入最近简历、投递公司、成长条目、模板。
- 支持直接打开简历、进入投递详情、开始 JD 定制、切换模板、导出。
- 按上下文和最近使用排序。

验收标准：

- 搜公司名能打开对应投递。
- 搜模板名能切换并预览。
- 搜“JD”能直接进入 JD 定制。

### 6.5 模板质量与视觉回归

负责人：前端、测试、设计

范围：

- 为 10 个模板补充布局意图和适用场景。
- 建立四类测试数据：中文、英文、长经历、长技能。
- 增加模板缩略图、预览、PDF 的一致性检查。
- 增加视觉回归测试。

验收标准：

- 每个模板有可感知差异，不只是颜色和间距变化。
- 长内容不重叠、不裁切。
- 缩略图和导出结果方向一致。

## 7. M3：平台化最小闭环

### 7.1 管理端真实化

负责人：前端、后端  
协作：产品、安全、测试

范围：

- 接入超管登录和角色权限。
- 菜单切换真实页面，不再只停留在 overview 静态面板。
- 接入真实用户/租户、简历、投递、平台请求、API Key、部署健康、配置同步、审计日志。
- 危险操作二次确认。

验收标准：

- `code/admin/src/App.tsx` 不再以静态数组作为主要数据源。
- 非超管不能访问敏感页面。
- 每个写操作记录操作者、对象、时间、结果。

### 7.2 平台 API 产品化

负责人：后端  
协作：管理端、产品、测试

范围：

- 发布 `/api/v1/resume-drafts` OpenAPI 文档。
- 提供 curl 示例、请求/响应示例、错误码、幂等说明。
- 增加 per-client API Key、scope、quota、rate limit。
- 管理端展示请求详情：client、request id、route、latency、状态、失败原因、生成 document id。

验收标准：

- 外部开发者可以只凭文档完成一次调用。
- 无 key、无 scope、超 quota 请求被正确拒绝。
- 平台请求可按 client、route、状态过滤。

### 7.3 数据库迁移

负责人：后端  
协作：前端、测试、运维

范围：

- 以 `code/backend/sql/001_initial_schema.sql` 为基础完善迁移。
- 引入 users、workspaces、resume_documents、job_applications、growth_entries、platform_requests。
- 所有实体增加 user/workspace ownership。
- 增加 revision/version，避免多端静默覆盖。
- 导入备份增加 `schemaVersion`、预览、校验报告、迁移日志。

验收标准：

- 不同用户数据隔离。
- 旧 JSON 数据可迁移。
- 并发更新不会 last-write-wins 静默覆盖。

## 8. 测试与发布要求

### 8.1 P0 必须补的测试

| 类型 | 覆盖范围 |
| --- | --- |
| 单元测试 | store 同步状态、JD 草稿应用、投递创建、成长条目模型 |
| 组件测试 | WelcomeDialog、Editor JD panel、Pipeline detail、TopBar sync state、Export precheck |
| E2E | 空白创建 -> 填核心字段 -> JD 定制 -> 应用章节 -> 创建投递 -> 导出 |
| 后端测试 | assistant draft、platform draft、application CRUD、sync failure、idempotency |
| 视觉回归 | 10 个模板、导出预检弹窗、投递详情抽屉 |

### 8.2 发布门槛

每个里程碑上线前必须满足：

- `npm run build` 通过。
- 前后端测试通过。
- P0 路径有至少 1 条 E2E 覆盖。
- 新增失败态有文案和重试路径。
- 新增 destructive action 有确认。
- 关键埋点已接入。

## 9. 团队分工建议

| 角色 | 主要职责 |
| --- | --- |
| 产品 | 路径验收、文案、指标、优先级裁剪、灰度反馈 |
| 设计 | JD 定制面板、导出预检、投递详情、空状态、窄屏策略 |
| 前端 | C 端主路径、状态管理、预检、管理端页面、测试 |
| 后端 | 草稿接口、同步状态支持、API key、平台日志、数据库迁移 |
| 测试 | E2E、视觉回归、导出样本、异常状态、权限验证 |
| 运维/安全 | API key 管理、审计、部署健康、备份和恢复 |

## 10. 关键风险与处理

| 风险 | 表现 | 处理方式 |
| --- | --- | --- |
| P0 被 P1/P2 稀释 | 同时做管理端、模板、数据库，C 端闭环延迟 | 每周只允许 P0 主线占用 70% 以上开发资源 |
| AI 过度承诺 | 用户期待真实智能优化，实际是规则生成 | 文案降噪，明确“基于 JD 和关键词生成草稿” |
| 同步状态复杂化 | 状态多但用户看不懂 | 顶部只展示一句话，详情放到同步中心 |
| PDF 预检误报 | 用户被阻塞但不知道如何修 | 每个问题必须给出自动修复或明确行动 |
| 数据库迁移过早 | 拖慢 C 端闭环 | M3 再迁移，M1/M2 只做必要接口兼容 |
| 管理端失控 | 静态面板继续扩张 | M3 前不再加静态卡片，新增必须接真实 API |

## 11. 第一周执行清单

建议从以下 10 件事开始：

1. 产品确认 P0 流程图和验收口径。
2. 前端梳理 `App.vue` 中已有编辑器 JD 逻辑，补可见 UI 或抽屉。
3. 前端删除/迁移 `WorkspacePanel.vue` 的隐藏 JD 双实现。
4. 前端补 WelcomeDialog 的导入入口和导入预览。
5. 前端定义空白简历引导清单和字段跳转。
6. 后端确认 assistant draft 响应是否能稳定返回 request id、match、strategy。
7. 前端定义导出预检检查项和第一版算法。
8. 前端/后端定义 sync operation 数据结构。
9. 测试补一条最小 E2E：空白 -> 编辑 -> JD 草稿 -> 投递记录。
10. 产品和测试建立四份导出样本：中文短、中文长、英文短、英文长。

## 12. 最终验收样例

M1 完成时，验收人员应使用下面脚本验证：

1. 清空本地数据，以新用户身份进入。
2. 选择空白创建，按引导填写个人信息、最近经历、技能。
3. 打开 JD 定制，输入目标公司、岗位和 JD。
4. 生成草稿，查看匹配分、命中关键词、before / after。
5. 只采纳个人简介和技能。
6. 从草稿创建投递记录。
7. 在投递页看到该记录、JD 快照、request id、匹配分。
8. 设置 follow-up 日期并记录一次进度。
9. 执行导出预检，修复或确认问题。
10. 导出 PDF，确认 activity、同步状态、PDF 内容都正确。

通过这条路径后，产品才算从“简历编辑器”推进到“求职工作台”的可交付版本。
