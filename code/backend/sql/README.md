# SQL

`001_initial_schema.sql` describes the target relational schema for moving the backend from local JSON-file storage to database-backed persistence.

The current service still runs without a database. Keep this directory as the source of truth for future migrations.
